# WorkBreath v1.5.5

This release fixes the square layer visible behind the rounded eye-care pre-break banner.

## Fix

- Moved the pre-break banner to a dedicated transparent HTML entry so it no longer inherits the main app canvas background.
- Added a real transparent gutter around the card and kept the shadow on the rounded card itself.
- Preserved the banner's content, countdown behavior, and screen-edge spacing.

## Validation

- All 433 frontend tests and the production build passed.
- GitHub Actions passed Rust `cargo check`, Clippy, and tests.
